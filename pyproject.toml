[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "slate"
version = "0.1.0"
description = "Minimal Slate compiler prototype"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["slatec"]

